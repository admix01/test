import { getChordPositions, getTriadPositions, parseShape, validatePlayableShape } from "./guitar-engine.js";

const chords = await fetch("../processed/chord_positions_by_name.json").then(r => r.json());
const triads = await fetch("../processed/triads_positions_standard_tuning.json").then(r => r.json());

console.log(getChordPositions(chords, "C", { maximumResults: 10 }));
console.log(getTriadPositions(triads, { root: "C", quality: "major", inversion: "first", strings: [3,2,1] }));
console.log(validatePlayableShape(parseShape("x.3.2.0.1.0")));
